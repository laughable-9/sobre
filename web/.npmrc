# @creit-tech/stellar-wallets-kit ships on JSR, not npm. Pointing the @jsr
# scope at JSR's npm-compat registry lets `npm install` resolve it transparently.
# smart-account-kit imports it as a regular dep; nothing in our code needs to
# know it's JSR-backed.
@jsr:registry=https://npm.jsr.io
